"""Rule library for Brick + Haystack semantic tagging.

YAML files in this directory are loaded by `brick_bacnet_mcp.tagger.Tagger`.
Users extend or override by editing these files or by pointing config at
local copies.
"""
